# Secrets — .env was previously committed with a live NOTION_API_KEY in it; it's untracked here
# but the key needs to be rotated in Notion since it's still in git history.
.env
.env.local

# Dependencies
node_modules/

# Build output — regenerated by `npm run build`, not source.
dist/

# Generated at build/dev time by scripts/generate-thumbnails.ts — fetched from Geoapify, not
# hand-authored; regenerates on demand and is safe to lose.
public/org-thumbnails/
src/data/org-thumbnails.json
